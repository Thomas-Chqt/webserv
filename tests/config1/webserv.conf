error_log webserv.log;

http {
    server {
        listen      8080;
        server_name localhost;

        location / {
            root  tests/config1/server/www;
            index index.html;
        }
    }
}