error_log nginx.log;

daemon off;

event {
}

http {
    server {
        listen      8090;
        server_name localhost;

        location / {
            root  test/config1/server/www;
            index index.html;
        }
    }
}