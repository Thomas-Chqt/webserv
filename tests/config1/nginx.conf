error_log nginx.log;

daemon off;

events {
}

http {
    server {
        listen      8090;
        server_name localhost;

        location / {
            root  tests/config1/server/www;
            index index.html;
        }
    }
}