error_log webserv.log;

http {
    server {
        listen      8080;
        server_name localhost;

        location / {
            root tests/cgiConfig/server;
            accepted_methods GET POST;
            accepted_cgi_extension .py <PYTHON_PATH>;
        }
    }
}